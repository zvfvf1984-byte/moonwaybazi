Edit `src/routes/api/bot.ts`:

- Line 79 tool description: «Передать диалог живому оператору…» → «Передать диалог оператору…»
- Line 105 system prompt: добавить явную инструкцию говорить «передал ваш запрос оператору» (без слова «живому»), чтобы модель не генерировала «живому оператору» в ответе клиенту.

Никаких других изменений — только текст промпта/описания инструмента.